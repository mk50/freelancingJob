
{
    "request": {
        "method": "POST",
        "header": [
            {
                "key": "Authorization",
                "value": "{{authtoken}}",
                "type": "text"
            },
            {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
            }
        ],
        "body": "{nc_id,user_id,submission_id,question_id,center_id,remark,images,asigned_to(commasepreated ids),due_date,question,type('Open', 'Completed', 'Closed', 'Rejected'),prevoius_nc_id,remark_id(for add blank)}",
        "url": "noncomplaince/save (id for update)"
    },

    "response" :
    {
        "title": "success",
        "message": "Saved Successfully",
        "data": [],
        "status": true
    }
}